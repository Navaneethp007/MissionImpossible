#include <iostream>
using namespace std;

int main() {
	int T,N;
	cin>>T;
	while(T-- >0)
	{
	    cin>>N;
	    int c=0;
	    for(int i=1; i<=N;i++)
	    {
	        if(i%2!=0)
	        {
	            int k=N-i+1;
	            c=c+(k*k);
	        }
	    }
	    cout<<c<<"\n";
	}
	return 0;
}
